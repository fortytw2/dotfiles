# runs on top of grml-zsh-config from normal arch packages

export GOPATH=~/go
export PATH=$PATH:/home/ian/.gem/ruby/2.1.0/bin:/home/ian/go/bin/

alias yolo='git rm -f * && git commit -am "yolo" && git push -f origin master'
